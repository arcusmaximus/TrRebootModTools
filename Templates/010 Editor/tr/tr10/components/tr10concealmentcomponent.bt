#define TR_VERSION 10
#include "../../trcommon.bt"

typedef struct
{
    float distance;
    int checkFrames;
} ConcealmentComponent <optimize=false>;

RefDefinitions refDefinitions;
ConcealmentComponent root <open=true>;