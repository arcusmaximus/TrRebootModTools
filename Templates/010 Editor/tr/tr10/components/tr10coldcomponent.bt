#define TR_VERSION 10
#include "../../trcommon.bt"

typedef struct
{
    float meterAmount;
    float meterDeduct;
    float deductPeriod;
    float damagePenalty;
    float damageInterval;
    float stageOnePercentage;
    float stageTwoPercentage;
    float stageThreePercentage;
} ColdComponent <optimize=false>;

RefDefinitions refDefinitions;
ColdComponent root <open=true>;